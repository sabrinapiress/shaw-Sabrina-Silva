import React from "react";

const ButtonsMatch = ()=> {
  //const [profilelList, setPerfilList] = [];

    return (
      <div>
        {/* <button key={profilelList.id} onClick={() => postChoosePerson(profilelList.id)}>X</button>
        <button>M</button> */}
      </div>
    );
  }
  
  export default ButtonsMatch